{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @shared-dynamic-routes/* --parallel start",
    "build": "lerna run --scope @shared-dynamic-routes/* build",
    "serve": "lerna run --scope @shared-dynamic-routes/* --parallel serve",
    "clean": "lerna run --scope @shared-dynamic-routes/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
