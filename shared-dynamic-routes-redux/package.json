{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @shared-dynamic-routes-redux/* --parallel start",
    "build": "lerna run --scope @shared-dynamic-routes-redux/* build",
    "serve": "lerna run --scope @shared-dynamic-routes-redux/* --parallel serve",
    "clean": "lerna run --scope @shared-dynamic-routes-redux/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "dependencies": {
    "react-redux": "^7.2.2",
    "redux": "^4.0.5"
  }
}
